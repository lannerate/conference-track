group 'conference.track.system'
version '1.0'

apply plugin: 'java'

sourceCompatibility = 1.8
version='1.0'

jar{
    manifest{
        attributes 'Main-Class': 'conf.track.sys.ConferenceApp'
    }
}

repositories {
    mavenCentral()
}

dependencies {
    testCompile group: 'junit', name: 'junit', version: '4.11'
}
